import React from 'react';
import ReactDOM from 'react-dom';

import { ReactLibTs } from '../src/components/ReactLibTs';

ReactDOM.render(
    <ReactLibTs />,
    document.getElementById('example')
);
